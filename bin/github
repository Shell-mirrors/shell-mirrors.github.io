#!/bin/bash
if [ -z "$(expect -v | grep "expect version [0-9]\.[0-9]\{1,3\}")" ];then
	apt-get install -y expect
fi
echo user=github > /tmp/logininfo.conf
if [ $UID -ne 4998 ];then
	clear
	login-script
else
	echo '已经是github用户了无需重复登录...'
fi
